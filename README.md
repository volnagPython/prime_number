# Tkinter GUI Project

## Description
Tkinter project that runs  a Python script to verify 
if the integer number entered by a user is prime.

## Tech stack
- Python
- TkInter


## Setup
```bash
git clone <repo_url>
cd prime_number
python -m venv venv
source venv/bin/activate
python primeNumberGUI_g.py

#Aug.31,2026
#Програмка з TKinter інтерфейсом
#що перевіряє чи введене ціле є простим.